<!-- 报名进度条 -->
<view class="progress-section">
  <view class="progress-label">报名进度</view>
  <view class="progress-bar">
    <view class="progress-fill" style="width: {{progressPercent}}%"></view>
  </view>
  <view class="progress-text">{{signedCount}}/{{playerCount}} 已完成</view>
</view>
